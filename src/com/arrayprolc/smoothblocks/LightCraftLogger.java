package com.arrayprolc.smoothblocks;

public class LightCraftLogger {

	public static Main plugin;

	public LightCraftLogger(Main instance) {
		plugin = instance;
	}
	/*
	 * public final Logger logger = plugin.logger.getLogger("Minecraft");
	 * 
	 * public void enabled(boolean enabled) { if(enabled) {
	 * PluginDescriptionFile pdfFile = enabled plugin.getDescription(); } }
	 */
}
